import { Component, ReactElement } from 'react';
import { Router } from '../../router/Router';

class App extends Component {
  render(): ReactElement {
    return <Router />;
  }
}

export default App;
